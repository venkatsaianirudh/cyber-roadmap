Monday - Couldn't work need to make it up thruout the week.

Tuesday - I have learned the basic commands to navigate the file system on linux thru terminal such as the ls, mv, rm, mkldir, cp, cd. i have also learned how to give and remove permissions using chmod and the general understanding for rwe values 6,4, and 1.  I understood git commands and what they do. And ended the day with some python if/else practice.

Wednesday - I have learned more linux commands such as the ps, top, ls -a and ls -l. I ahve revised on the previous linux commands from tuesday by moving around the file systems without notes. understood git concepts such as repo/ commit history using commands; git logs - oneline, git logs, and git status. make 2 python scripts like the calculator and odd/even identrifier (the scripts are getting very easy since i have experience in python have to take on tougher tasks). Ended the day with updating thje README.md . I have caught up to my weekly timeline for skipping monday, worked on how HTTPS request/responses are handled, headers of request/responses. what different types of network status codes are. HTTPS Methods. I understood Burpsuite ands its interceptor. and have done my first lab based on it.

Thursday- I have understood the importance of cookies and sessions. Learned how to view cookies in a request by doing a lab thru burpsuite. A network header has information like the method it used, path, the host url, the user agent and the cookies used in the request. 

Friday- I practiced core Linux navigation and file handling by moving around my repo, creating folders and files, copying, renaming, moving, and deleting them. I also practiced working with hidden files and nested folders, and learned how paths work when moving files between directories. I practiced reading file contents in different ways, including viewing full files, scrolling through them, and checking only the beginning or end of files. I also created and edited notes files directly for practice. I worked on Linux permissions by creating a test file, checking its permissions, changing them to private, making the file executable, and then changing it back to a normal readable file. I also practiced process-related commands by checking currently running processes and using the live process viewer, so I could understand the difference between a one-time snapshot and a continuously updating process list. On the Git side, I checked repo status, created and edited note files, staged changes, committed them with messages, viewed commit history, and pushed updates to GitHub. I also did a combined drill where I mixed file creation, hidden files, renaming, copying, moving files into other folders, reading notes files, checking permissions, checking processes, and then saving everything with Git and pushing it online.
##What I got wrong
I got confused about moving files between folders when the destination was outside my current directory. I also mixed up files and folders at first, and needed to better understand relative paths. I was also unsure how to write content into files directly from the terminal.
## What still feels slow
Using paths confidently still feels slow, especially when moving files between directories. Permissions with `chmod` are still not fully natural yet. Git is working, but I still need more repetition so the workflow feels automatic.
